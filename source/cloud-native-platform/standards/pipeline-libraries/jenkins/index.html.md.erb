---
title: Jenkins
last_reviewed_on: 2025-07-02
review_in: 12 months
weight: 3
---

# <%= current_page.data.title %>

This section explains the common delivery workflow used at HMCTS.

For the canonical setup sequence, follow [Starting a new component](../../../new-component/), which takes you from a new repository through non-production validation in the required order.

After onboarding, use these references to understand and operate the pipeline:

<% current_page.children.sort_by { |page| page.data.weight || Float::INFINITY }.each do |page| %>
<% if page.data.title && !page.data.title.strip.empty? %>
- [<%= page.data.title %>](<%= page.url %>)
<% end %>
<% end %>
